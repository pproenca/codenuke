{
  "name": "codenuke-reimagined",
  "version": "0.0.0",
  "private": true,
  "description": "Autonomous behavior-preserving code reduction — Effect-TS reimagining.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.1",
    "fast-check": "^4.8.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
