{
  "name": "@codenuke/scorer",
  "version": "0.1.0",
  "description": "The immutable judge's keep/revert decision (gates + value) — modernized pure core of loop/scorer.mjs.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "exports": { ".": { "types": "./dist/scorer.d.ts", "import": "./dist/scorer.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepack": "pnpm -s build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@codenuke/artifacts": "workspace:*",
    "@codenuke/config": "workspace:*",
    "@codenuke/exec": "workspace:*",
    "@codenuke/measure": "workspace:*",
    "@codenuke/substrate": "workspace:*"
  },
  "devDependencies": { "typescript": "^6.0.3", "vitest": "^4.1.6" }
}
