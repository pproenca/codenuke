{
  "name": "@codenuke/exec",
  "version": "0.1.0",
  "private": true,
  "description": "Safe subprocess substrate (execFile arg-arrays, no shell) for codenuke — CWE-78 remediation of loop/shell.mjs.",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/exec.d.ts",
      "import": "./dist/exec.js"
    }
  },
  "scripts": {
    "build": "pnpm -s clean && tsc -p tsconfig.build.json",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "prepack": "pnpm -s build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "engines": {
    "node": ">=22"
  }
}
