{
  "name": "codenuke",
  "version": "0.4.0",
  "description": "Autonomous behavior-preserving code reduction.",
  "license": "MIT",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "codenuke": "dist/cli.cjs"
  },
  "files": [
    "dist",
    "scripts",
    "docs",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "codenuke": "node dist/cli.cjs",
    "codenuke:build": "pnpm -s build && node dist/cli.cjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "node scripts/bundle-cli.mjs",
    "prepack": "pnpm -s build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.1",
    "typescript": "^6.0.3"
  }
}
