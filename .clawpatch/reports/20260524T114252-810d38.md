# clawpatch report

findings: 27

## high: Published export points at a non-existent build artifact

id: fnd_sig-feat-library-ef3da57849-806d_44ce23e8a5
category: build-release
confidence: high
triage: risk
status: open
feature: Node package @codenuke/calibrate (feat_library_ef3da57849)

evidence:
- packages/calibrate/package.json:10-14
- packages/calibrate/tsconfig.build.json:3-14
- packages/calibrate/src/main/calibrate.ts:1

The package export declares ./dist/calibrate.js and ./dist/calibrate.d.ts, but the build config emits files from rootDir src/main into outDir dist. The included source file is src/main/calibrate.ts, so tsc will emit dist/calibrate.js only if the compiler root is src/main and the file is directly inside it; this part is consistent. However the package name is marked private and this feature is identified as a Node package. If it is intended to be packed or depended on as @codenuke/calibrate, private:true prevents publication and npm pack consumers cannot treat it as a public package. This is a release hazard for the feature as described, but may be intentional if all packages/* are strictly internal.

recommendation:
Clarify whether @codenuke/calibrate is internal-only. If it should be published or installed independently, remove private:true and ensure the workspace release process includes it. If it is internal-only, keep private:true and do not classify it as a public package artifact.

test analysis:
The runtime and math tests import through the workspace path mapping and exercise source code, not npm publication behavior or package publishability.

suggested regression test:
Add a packaging smoke test that runs npm pack for packages/calibrate only if it is intended to be publishable, then imports the packed package from a temporary install.

minimum fix scope:
packages/calibrate/package.json

## high: Runtime cost snapshot ignores untracked source files

id: fnd_sig-feat-library-7fcb82f3a6-4438_c6cb759aaa
category: bug
confidence: high
triage: confirmed-bug
status: open
feature: Node package @codenuke/changecost (feat_library_7fcb82f3a6)

evidence:
- packages/changecost/src/main/changecost.ts:292-305 (snapshotWorktree)
- packages/changecost/src/main/changecost.ts:401-407 (runChangeCostCommand)
- packages/changecost/src/main/changecost.ts:420-423 (runChangeCostCommand)
- packages/changecost/src/test/changecost-runtime.test.ts:379-390

The runtime allows implementer changes to non-test source files, including untracked additions, but `snapshotWorktree` builds both baseline and post-implementation maps with `git ls-files` only. Untracked source files created by an implementer are therefore omitted from the after snapshot unless they were staged, so `editCost` will not charge their tokens or regions. A benchmark that passes by adding `src/new.ts` can be reported as zero or heavily undercounted cost, corrupting Vhat.

recommendation:
Include untracked, non-ignored source files in the post-implementation snapshot, for example by using `git ls-files -z --cached --others --exclude-standard -- <srcDir>` or equivalent, while preserving deletion handling for tracked files.

test analysis:
The pure `editCost` tests cover added files when the maps already contain them, but the runtime benchmark test modifies only an existing tracked `src/index.ts`; it never exercises an implementer-created untracked source file.

suggested regression test:
Add a runtime benchmark where the scripted implementer writes a new `src/added.ts`, the acceptance command requires that file/content, and the resulting artifact must report a nonzero edit cost including the added file.

minimum fix scope:
Update runtime source snapshot collection and add one runtime regression test for untracked source additions.

## medium: baseline resolution failure escapes the command-result contract

id: fnd_sig-feat-library-c26682e800-a764_3f8703754c
category: api-contract
confidence: high
triage: contract-mismatch
status: open
feature: Node source packages/fence/src (feat_library_c26682e800)

evidence:
- packages/fence/src/main/runtime.ts:109-116 (auditFence)
- packages/fence/src/main/runtime.ts:219-227 (auditFence)
- packages/fence/src/main/runtime.ts:285-291 (runFenceCommand)

runFenceCommand exposes a FenceCommandResult-returning API, and auditFence handles later operational failures by returning exitCode/stdout/stderr. However the initial git rev-parse for the configured baseline runs before the try/catch. If the baseline ref is missing or git errors, run() can throw out of auditFence, so callers get a rejected promise instead of the documented command result shape. That is inconsistent with the rest of the runtime error handling and can crash the CLI path instead of producing a controlled failure.

recommendation:
Move baseline command planning and baselineSha resolution inside the existing try/catch, or add a narrow try/catch around rev-parse that returns a FenceCommandResult with exitCode 1 and stderr.

test analysis:
The included tests only exercise pure mutation-core exports from @codenuke/fence. They do not import ./runtime or cover auditFence/runFenceCommand error paths.

suggested regression test:
Add a runtime test that stubs or configures an invalid baseline and asserts runFenceCommand([]) resolves to { exitCode: 1, stderr: ... } rather than rejecting.

minimum fix scope:
packages/fence/src/main/runtime.ts auditFence baseline resolution error handling

repro:
Configure an invalid baseline ref, then call runFenceCommand([]). The rev-parse at lines 109-115 executes before the catch block starts, so the thrown error bypasses the FenceCommandResult return path.

## medium: Published changelog does not include the package version being released

id: fnd_sig-feat-cli-command-e8cabc5dc0-_b5e0f3a614
category: build-release
confidence: high
triage: risk
status: open
feature: Node package codenuke (feat_cli-command_e8cabc5dc0)

evidence:
- apps/cli/package.json:2-3
- apps/cli/CHANGELOG.md:3-11
- apps/cli/README.md:77-79

The package manifest and README identify the public package as codenuke@0.4.0, but the published changelog's newest entry is 0.2.1. Because CHANGELOG.md is included in the npm package, consumers installing or auditing the 0.4.0 release will see no release notes for the version they received. This is a release packaging/API documentation mismatch rather than just stale internal docs because the manifest explicitly ships CHANGELOG.md in the tarball.

recommendation:
Add a 0.4.0 changelog entry before publishing, or align the manifest version with the latest documented release if 0.4.0 is not intended to ship yet.

test analysis:
No linked tests or release smoke checks assert that the package version has a corresponding changelog entry.

suggested regression test:
Add a packaging/release check that reads apps/cli/package.json version and fails if apps/cli/CHANGELOG.md lacks a matching top-level version heading.

minimum fix scope:
apps/cli/CHANGELOG.md

## medium: Root source directories allow docs and package metadata to survive proposer edits

id: fnd_sig-feat-library-b95d0a9ac8-477c_e2eede7f21
category: bug
confidence: high
triage: confirmed-bug
status: open
feature: Node source packages/orchestrator/src (feat_library_b95d0a9ac8)

evidence:
- packages/orchestrator/src/main/orchestrator.ts:278-286 (isAllowedReducePath)
- packages/orchestrator/src/main/runtime.ts:254-258 (changedSource)
- packages/orchestrator/src/test/orchestrator.test.ts:331-339

When srcDir is configured as ".", isUnderSourceDir returns true for every path, and isAllowedReducePath then accepts any .js/.ts/.jsx/.tsx/.mjs/.cjs file that is not a declaration or test file. changedSource relies on this predicate to decide which modified files count as source. In a monorepo or repo-root source configuration, a reducer can modify build scripts, config files, docs examples, benchmark helpers, or other non-source JavaScript/TypeScript files outside the intended reduction surface, and those edits will be scored as legitimate source changes instead of being rejected as out-of-surface changes. The existing tests cover normal srcDir="src" and node_modules, but not the root-source case for common non-source paths such as package.json-adjacent config files or scripts.

recommendation:
For srcDir=".", intersect allowed reduce paths with configured detected regions or explicitly reject known non-source roots and config/build files. Alternatively make region detection produce the authoritative allowlist and have changedSource validate against that allowlist instead of treating every code-like file in the repo as source.

test analysis:
The path-surface tests only assert srcDir="src" behavior and do not exercise srcDir="." with files that are code-like but outside application source.

suggested regression test:
Add tests for isAllowedReducePath or the higher-level reduce-surface check showing that, with srcDir=".", package/build/test tooling files such as vitest.config.ts, scripts/release.ts, and benchmark helpers are rejected unless they are in a detected source region.

minimum fix scope:
Tighten reduce path authorization in orchestrator.ts and update the caller/tests that rely on it.

repro:
Configure CN_SRC_DIR=. and let a reduce proposal edit vitest.config.ts or scripts/release.ts. changedSource will include the path because git diff is scoped to "." and isAllowedReducePath(path, ".") returns true for those extensions, so the candidate can be scored as an allowed reduce edit.

## medium: Untracked source files are invisible to score and accept

id: fnd_sig-feat-library-191f3a3776-b0fc_5ccf927836
category: bug
confidence: high
triage: confirmed-bug
status: open
feature: Node package @codenuke/scorer (feat_library_191f3a3776)

evidence:
- packages/scorer/src/main/scorer.ts:190-192
- packages/scorer/src/main/scorer.ts:245-247
- packages/scorer/src/main/scorer.ts:365-371
- packages/scorer/src/main/scorer.ts:412-421
- packages/scorer/src/test/scorer-runtime.test.ts:436-459

Candidate discovery uses `git diff -z --name-only HEAD -- srcDir`, which does not report untracked files. As a result, a candidate that only adds a new source file is treated as a clean worktree, and a candidate that modifies tracked source plus adds untracked source is scored and accepted without measuring or committing the new file. This can produce a misleading keep/reject decision because tests may pass with the untracked file present, while the accepted commit omits it. The accept test verifies that non-source package.json edits stay uncommitted, but it does not cover untracked source files.

recommendation:
Include untracked source files in candidate discovery, for example by combining the tracked diff with `git ls-files -z --others --exclude-standard -- srcDir`, then measure and stage the union of source paths.

test analysis:
The existing accept test mutates a tracked source file and a non-source package.json file, but it never creates an untracked source file that should participate in scoring and acceptance.

suggested regression test:
Add a runtime test that creates an initialized repo, writes a new untracked `src/*.ts` file in the scorer worktree, and verifies `score --json` reports it and `accept` commits it when gates pass.

minimum fix scope:
Update `changedSource` discovery and the accept/score paths that consume it; add targeted runtime coverage for untracked source additions.

## medium: Worktree cleanup only removes the root node_modules link

id: fnd_sig-feat-library-eb817b1790-7be8_38a5721f17
category: bug
confidence: high
triage: confirmed-bug
status: open
feature: Node source packages/substrate/src (feat_library_eb817b1790)

evidence:
- packages/substrate/src/main/worktree.ts:68-76 (unlinkWorktreeNodeModules)
- packages/substrate/src/main/worktree.ts:80-91 (removeWorktree)
- packages/substrate/src/test/worktree.test.ts:80-106

linkWorktreeNodeModules explicitly handles the root node_modules plus nested workspace node_modules paths, but removeWorktree only removes `${worktree}/node_modules` before invoking `git worktree remove --force`. If linkWorktreeNodeModules created nested symlinks such as `packages/foo/node_modules`, those untracked symlinks can remain in the worktree and cause `git worktree remove` to fail or leave directories behind. The catch block then suppresses the failure, so callers can believe cleanup succeeded while the worktree still exists.

recommendation:
Call unlinkWorktreeNodeModules(repo, worktree) from removeWorktree, or otherwise remove every path returned by worktreeNodeModulesPaths(repo) before running git worktree remove. Avoid swallowing the final removal failure silently if the worktree still exists.

test analysis:
The integration tests only create and validate a root node_modules symlink, and the remove test removes a worktree that never had nested node_modules symlinks linked into it.

suggested regression test:
Create a fixture repo with a nested workspace node_modules directory, call linkWorktreeNodeModules, then removeWorktree, and assert the worktree path no longer exists.

minimum fix scope:
Update removeWorktree to clean the same node_modules symlink set that linkWorktreeNodeModules creates, and add an integration test for nested workspace node_modules cleanup.

## medium: Artifacts remain usable when the configured baseline cannot be resolved

id: fnd_sig-feat-library-0b9bc4b759-120c_476a2e378c
category: security
confidence: medium
triage: risk
status: open
feature: Node source packages/artifacts/src (feat_library_0b9bc4b759)

evidence:
- packages/artifacts/src/main/artifacts.ts:128-135 (fenceArtifactStatus)
- packages/artifacts/src/main/artifacts.ts:159-171 (calibrationArtifactStatus)

Both freshness checks call resolveRef, but only reject a mismatched baselineSha when the configured baseline resolves successfully. If git rev-parse fails because the repo is not a git repository, the baseline ref is invalid, or git cannot verify it, baselineSha-bearing artifacts skip the stale checks and can still pass metadata validation. These functions are documented as fail-closed gates for stale or invalid artifacts, so inability to verify the trusted baseline should not make a recorded SHA trustworthy.

recommendation:
Treat an unresolved configured baseline as unusable whenever the artifact relies on baselineSha freshness. Return a stale or invalid reason before accepting fence or calibration artifacts, and add the same fail-closed behavior anywhere baselineSha freshness is required.

test analysis:
The existing tests create a valid temporary git repository and always use baseline HEAD, so resolveRef succeeds in the covered paths. There is no test for an invalid baseline ref or non-git repo with a baselineSha-bearing artifact.

suggested regression test:
Create a temp directory with a syntactically valid fence and calibration artifact containing baselineSha, configure baseline to a missing ref such as refs/heads/nope, and assert the statuses are unusable rather than accepted.

minimum fix scope:
Update fenceArtifactStatus and calibrationArtifactStatus baseline resolution handling, plus focused tests for unresolved baseline refs.

## medium: Backslash traversal bypasses path-safety checks on Windows

id: fnd_sig-feat-library-7fcb82f3a6-b35c_682d2bd745
category: security
confidence: medium
triage: risk
status: open
feature: Node package @codenuke/changecost (feat_library_7fcb82f3a6)

evidence:
- packages/changecost/src/main/changecost.ts:192-201 (assertSafeSourcePath)
- packages/changecost/src/main/changecost.ts:222-234 (safeWorktreePath)
- packages/changecost/src/main/changecost.ts:409-411 (runChangeCostCommand)
- packages/changecost/src/test/changecost-runtime.test.ts:291-307

The code imports platform-specific `node:path`, but the traversal and symlink checks hard-code `/` when detecting parent paths and splitting path components. On Windows, `relative(root, outside)` uses backslashes, so a path like `..\outside.ts` would not match `back === '..'` or `back.startsWith('../')`; it can then be used for the hidden acceptance-test write. The tests assert the intended traversal rejection but only cover forward-slash traversal and symlink parents.

recommendation:
Normalize and reject both `/` and `\` traversal separators for untrusted relative inputs, or use platform-aware checks with `path.sep` plus explicit rejection of alternate separators where appropriate. Add Windows-style traversal cases for `safeWorktreePath` and `assertSafeSourcePath`.

test analysis:
The current path-safety tests use `/tmp/outside.ts`, `../outside.ts`, and a forward-slash child path; they do not cover Windows-style `..\` traversal.

suggested regression test:
Assert that `safeWorktreePath(worktree, '..\\outside.ts')` throws and that `changeCostGitCommandPlan` rejects a `srcDir` containing `..\\` traversal.

minimum fix scope:
Harden path validation helpers and extend the path-safety tests with backslash traversal cases.

## medium: Build script can package stale dist artifacts

id: fnd_sig-feat-release-05eeb886b2-c00f_3d04130576
category: build-release
confidence: medium
triage: risk
status: open
feature: Package script build (@codenuke/orchestrator) (feat_release_05eeb886b2)

evidence:
- packages/orchestrator/package.json:20-25 (build)

The package only includes the generated dist directory in its published file set, but the build script runs tsc directly without first removing prior outputs. TypeScript does not delete emitted files that correspond to renamed or removed source files, so a local or CI packaging run can leave obsolete JavaScript or declaration files in dist and include them in the package artifact.

recommendation:
Make the package build remove dist before compiling, for example by adding a clean step and running it before tsc, or by using an existing workspace clean helper if one exists.

test analysis:
No linked tests are included for this package script, and unit tests would not normally catch stale files left in dist across successive builds.

suggested regression test:
Add a packaging smoke test that creates a stale file in packages/orchestrator/dist, runs the package build/prepack path, and asserts the stale file is absent from the final packed contents.

minimum fix scope:
Update packages/orchestrator/package.json so the build/prepack path cleans dist before invoking tsc.

## medium: Default subprocess output buffer allows very large memory allocation

id: fnd_sig-feat-library-d00fdcd48f-ba13_125a58aadd
category: performance
confidence: medium
triage: risk
status: open
feature: Node source packages/exec/src (feat_library_d00fdcd48f)

evidence:
- packages/exec/src/main/exec.ts:40-47 (run)
- packages/exec/src/test/exec.test.ts:136-139

run() overrides execFileSync's buffer limit to 1 GiB for every caller. Any child command that writes a large or unbounded stdout/stderr stream can force the CLI process to buffer up to that amount synchronously before failing, which is a realistic denial-of-service risk for a subprocess substrate likely used around git and other external tools. The included tests only verify small outputs and nonzero exits, so they do not establish that such a large default is required.

recommendation:
Use Node's safer default maxBuffer or a project-specific bounded default that reflects expected command output, and require exceptional large-output callers to opt in with options.maxBuffer.

test analysis:
The tests exercise only tiny stdout/stderr payloads and do not assert the default maxBuffer behavior or memory ceiling.

suggested regression test:
Add a test that runs a command producing output larger than the chosen default and asserts run() fails before buffering an excessive amount, while still allowing an explicit maxBuffer override when needed.

minimum fix scope:
Change the default maxBuffer in run() and update or add tests documenting the intended limit.

repro:
Call run("node", ["-e", "process.stdout.write('x'.repeat(1024 * 1024 * 1024))"]) from a caller without a tighter maxBuffer; the parent process attempts to buffer up to the configured 1 GiB limit synchronously.

## medium: Duplicate-window signatures can collide because token keys are concatenated without separators

id: fnd_sig-feat-library-dcf3269a22-82db_4e45126ade
category: bug
confidence: medium
triage: risk
status: open
feature: Node package @codenuke/measure (feat_library_dcf3269a22)

evidence:
- packages/measure/src/main/measure.ts:122-128 (duplicateMass)
- packages/measure/src/test/measure.test.ts:53-56
- packages/measure/src/test/measure.test.ts:65-70

duplicateMass builds each 12-token window key with window.map(...).join(""). Individual token keys have variable length and no escaping or delimiter, so two different token sequences can produce the same string key. That can falsely inflate dupMass by treating unrelated windows as duplicates, corrupting the RULE-005 measurement. The existing tests only check obvious repeated blocks and legacy-oracle parity on fixed examples, so they would not catch a crafted signature collision.

recommendation:
Use an unambiguous serialization for window keys, such as JSON.stringify(window.map(token => token.key)), or join with a delimiter that is length-prefixed/escaped so adjacent token keys cannot merge ambiguously.

test analysis:
The tests assert duplicate mass for repeated identical blocks and compare a few realistic fixtures to the legacy oracle, but they do not include non-identical windows with ambiguous concatenated token-key boundaries.

suggested regression test:
Add a test with two non-identical 12-token windows whose current concatenated key collides, and assert dupMass remains zero after switching to an unambiguous key format.

minimum fix scope:
Change duplicateMass window key construction and add a focused regression test in packages/measure/src/test/measure.test.ts.

## medium: Fence validator accepts array-shaped region maps

id: fnd_sig-feat-library-d2cac6910e-0c3a_d517bd8be0
category: security
confidence: medium
triage: risk
status: open
feature: Node package @codenuke/artifacts (feat_library_d2cac6910e)

evidence:
- packages/artifacts/src/main/artifacts.ts:123-139 (fenceArtifactStatus)
- packages/artifacts/src/main/artifacts.ts:91-118 (validFenceRegions)
- packages/artifacts/src/test/artifacts.test.ts:83-134

fenceArtifactStatus only checks that artifact.regions is a non-null object before casting it to a record. Arrays satisfy that runtime check, and validFenceRegions then uses Object.keys/Object.values, so a JSON array containing valid-looking region objects can be accepted as a usable fence artifact even though the expected contract is a named region map. This weakens the fail-closed safety gate because malformed/tampered artifact shape can be trusted.

recommendation:
Reject arrays for artifact.regions before calling validFenceRegions, for example by reusing isRecord(artifact.regions). Consider also requiring region names to be non-empty strings if region keys are part of the contract.

test analysis:
The fence tests cover missing files, valid object-shaped regions, stale SHA, bad metadata, tampered Wilson stats, and bad admissible flags, but they do not cover malformed container types such as regions arrays.

suggested regression test:
Add a fenceArtifactStatus test that writes a valid fence artifact with regions converted from an object to an array of the same region rows and expects usable=false with reason invalid-regions or invalid-metadata.

minimum fix scope:
packages/artifacts/src/main/artifacts.ts plus a focused fenceArtifactStatus regression test.

repro:
Write a fence-fidelity.json whose regions value is an array containing the same valid region objects used by the tests; the Object.values-based validation can still pass and return usable=true.

## medium: Invalid numeric overrides can produce NaN or unsafe runtime values

id: fnd_sig-feat-service-bf020e7473-b7c3_7317e43fc1
category: bug
confidence: medium
triage: risk
status: open
feature: Node package @codenuke/config (feat_service_bf020e7473)

evidence:
- packages/config/src/main/config.ts:321-334 (loadConfig)
- packages/config/src/test/config.test.ts:100-115
- packages/config/src/test/config.test.ts:145-210

CN_FENCE_LB and CN_TIMEOUT are converted with Number(...) and returned without validating that the result is finite and meaningful. Values such as CN_FENCE_LB=abc produce NaN, CN_FENCE_LB=Infinity produces Infinity, and CN_TIMEOUT=0 or a negative/NaN value can be returned as proposerTimeoutMs. These fields drive scoring thresholds and proposer timeout behavior, so malformed environment or file config can silently disable expected bounds or break later consumers instead of failing closed. The nearby CN_WEIGHTS parsing explicitly rejects malformed and non-finite numeric values, which indicates this package already treats invalid numeric config as an error for similar scoring inputs.

recommendation:
Parse numeric config through a shared helper that rejects non-finite values and, where applicable, enforces domain bounds such as fenceLB between 0 and 1 and timeout greater than 0. Apply it to env and codenuke.loop.json values for fenceLB and proposerTimeoutMs.

test analysis:
The env override test only exercises valid numeric strings for CN_FENCE_LB and CN_TIMEOUT. The fail-closed tests cover CN_WEIGHTS and codenuke.loop.json weights only, not the other numeric fields.

suggested regression test:
Add tests that CN_FENCE_LB, codenuke.loop.json fenceLB, CN_TIMEOUT, and codenuke.loop.json proposerTimeoutMs reject malformed, non-finite, and out-of-range values with clear errors.

minimum fix scope:
Update loadConfig numeric parsing for thresholds.fenceLB and proposerTimeoutMs, plus focused tests in packages/config/src/test/config.test.ts.

repro:
Call loadConfig({ CN_PROGRAM: '/fixed/program.md', CN_FENCE_LB: 'not-a-number', CN_TIMEOUT: 'NaN' }, repoWithPackageAndSrc). The returned config has thresholds.fenceLB === NaN and proposerTimeoutMs === NaN.

## medium: JSX files are parsed and scanned as plain TypeScript unless they end in .tsx

id: fnd_sig-feat-library-7c1de51af7-e49b_0ed88bae33
category: bug
confidence: medium
triage: risk
status: open
feature: Node source packages/measure/src (feat_library_7c1de51af7)

evidence:
- packages/measure/src/main/measure.ts:28-35 (scriptKind)
- packages/measure/src/main/measure.ts:89-95 (tokenStream)
- packages/measure/src/test/measure.test.ts:19-37

The module recognizes JavaScript/JSX test filenames via the [tj]sx? test-file regex, but JSX parsing is only enabled for .tsx names. Non-test .jsx files therefore go through ts.ScriptKind.TS and LanguageVariant.Standard, so JSX syntax is tokenized/parsed as non-JSX TypeScript. That can skew L and dupMass for JSX sources even though the filename handling implies JS/JSX inputs are in scope.

recommendation:
Treat .jsx as JSX in both scriptKind and scanner variant, for example by replacing isTsx with an isJsxLike helper that matches .tsx and .jsx, and selecting ScriptKind.JSX for .jsx.

test analysis:
The characterization matrix includes .ts sources and a .test.ts exclusion case only; it has no .jsx or .tsx source case that would expose JSX parsing/scanning differences.

suggested regression test:
Add a measurement case for a .jsx file containing JSX markup and compare it to the legacy oracle or to the same content under a .tsx filename where applicable.

minimum fix scope:
Update extension detection in packages/measure/src/main/measure.ts and add a focused JSX measurement test in packages/measure/src/test/measure.test.ts.

## medium: JSX files are selected for audits but parsed as non-JSX TypeScript

id: fnd_sig-feat-library-a7f2960d5e-7b6d_d4f4fca10a
category: bug
confidence: medium
triage: risk
status: open
feature: Node package @codenuke/fence (feat_library_a7f2960d5e)

evidence:
- packages/fence/src/main/fence.ts:63-70 (collectSites)
- packages/fence/src/main/fence.ts:134-135 (isSourceFile)
- packages/fence/src/test/fence.test.ts:8-30
- packages/fence/src/test/fence-runtime.test.ts:200-230

The runtime includes .jsx files as source candidates, but collectSites only switches to ScriptKind.TSX for names ending in .tsx. A .jsx file containing JSX syntax is therefore parsed as plain TypeScript, which can produce a malformed AST and miss mutation sites or compute unreliable spans inside JSX expressions. Existing tests cover .tsx path filtering but do not call collectSites on JSX syntax, so this mismatch is not exercised.

recommendation:
Choose ScriptKind.JS/JSX/TS/TSX from the file extension, using JSX for both .jsx and .tsx files and JS for .js/.mjs/.cjs where appropriate.

test analysis:
The collectSites tests only use a .ts filename, and runtime discovery tests only verify that .tsx-like paths are retained; no test feeds JSX source to collectSites.

suggested regression test:
Add a collectSites test for a .jsx file such as `export const C = ({a,b}) => <div>{a < b}</div>;` and assert that the `<→>` site is found and applyMutant produces valid mutated source.

minimum fix scope:
Update collectSites script-kind selection and add focused parser coverage for .jsx/.tsx inputs.

## medium: Typecheck script can pass without running any type checks

id: fnd_sig-feat-release-a235aa99b1-80f0_c62e665bd2
category: build-release
confidence: medium
triage: risk
status: fixed
feature: Package script typecheck (feat_release_a235aa99b1)

evidence:
- package.json:27 (typecheck)
- package.json:34

The published root package advertises a `typecheck` command, but the command only recursively asks filtered child packages to run their own `typecheck` scripts and uses `--if-present`. That means the root command can exit successfully when selected packages have no `typecheck` script, and it does not directly invoke TypeScript despite TypeScript being a dev dependency. This can make CI/release validation report success while no package was actually typechecked.

recommendation:
Make the root `typecheck` script fail unless the intended TypeScript checks actually run, for example by invoking the concrete `tsc --noEmit` commands or removing `--if-present` once every package is expected to define `typecheck`.

test analysis:
No linked tests are included for the package script behavior, and package scripts are not validated by the shown package metadata.

suggested regression test:
Add a packaging or CI smoke test that runs `pnpm typecheck` and asserts the expected TypeScript projects are checked rather than silently skipped.

minimum fix scope:
Update the root `typecheck` script and, if needed, add/standardize package-level `typecheck` scripts.

## medium: wilson accepts invalid z values that can silently corrupt intervals

id: fnd_sig-feat-library-42aa292aa1-a3dd_7e80f8a883
category: api-contract
confidence: medium
triage: contract-mismatch
status: open
feature: Node package @codenuke/stats (feat_library_42aa292aa1)

evidence:
- packages/stats/src/main/stats.ts:32-39 (wilson)
- packages/stats/src/main/stats.ts:45-55 (wilson)
- packages/stats/src/test/stats.test.ts:252-258

The public API exposes z as a caller-supplied normal quantile, and the function explicitly fails loud for invalid k/n to avoid NaN-poisoned safety-gate decisions. However, z is not validated before it is used in interval arithmetic. A non-finite z produces NaN bounds, while a negative z can invert the half-width and violate the expected lo <= p <= hi invariant. The validation tests cover invalid k/n inputs but not invalid z, so a bad configured quantile can still produce wrong-but-plausible gate data instead of failing loud.

recommendation:
Validate z in wilson, for example require Number.isFinite(z) && z >= 0 or z > 0 depending on the intended confidence-interval contract, and throw RangeError on invalid values before computing z2.

test analysis:
The invalid-input suite only asserts RangeError for k > n, negative k, non-integer n, and NaN k; it never passes an invalid custom z.

suggested regression test:
Add cases asserting wilson(5, 10, Number.NaN), wilson(5, 10, Number.POSITIVE_INFINITY), and wilson(5, 10, -1.96) throw RangeError, plus one valid custom z case if custom quantiles are supported.

minimum fix scope:
packages/stats/src/main/stats.ts and packages/stats/src/test/stats.test.ts

repro:
Call wilson(5, 10, Number.NaN) and observe NaN interval fields, or wilson(5, 10, -1.96) and observe inverted bounds relative to p.

## medium: Worktree cleanup can throw and mask the real failure

id: fnd_sig-feat-library-bd0051e6a0-1b84_b13f05874d
category: bug
confidence: medium
triage: risk
status: open
feature: Node source packages/changecost/src (feat_library_bd0051e6a0)

evidence:
- packages/changecost/src/main/changecost.ts:362-370 (runChangeCostCommand)
- packages/changecost/src/main/changecost.ts:455-457 (runChangeCostCommand)

The command calls cleanupWorktree() both in normal error paths and again inside the catch block without guarding cleanup failures. If addWorktree, green(), snapshotting, implementer execution, or artifact writing throws, then cleanupWorktree() in the catch can also throw and reject runChangeCostCommand instead of returning the RuntimeResult promised by its API. The same risk exists in the baseline-red branch, where cleanupWorktree() is called before returning. This makes bad error handling observable to callers and can hide the original failure message.

recommendation:
Wrap cleanupWorktree() calls in a best-effort helper that catches cleanup errors and appends them to stderr, while preserving the original failure return. Use that helper everywhere cleanup is part of an error/return path.

test analysis:
The included tests only exercise tokenize, lcsEditSize, editCost, verifyCost, buildImplementerPrompt, costOf, and meanChangeCost; they do not cover runChangeCostCommand or cleanup failure paths.

suggested regression test:
Mock removeWorktree to throw during a runChangeCostCommand failure path and assert the function resolves with exitCode 1 and includes the original error rather than rejecting.

minimum fix scope:
Add guarded cleanup handling inside runChangeCostCommand and update runtime tests for failure cleanup behavior.

repro:
Make removeWorktree fail during the catch path, for example by using a mocked @codenuke/substrate removeWorktree that throws after any earlier operation throws. runChangeCostCommand will reject instead of resolving { exitCode: 1, ... }.

## low: Custom negative z values produce inverted Wilson intervals

id: fnd_sig-feat-library-b8b0895f21-95f0_bdd603596b
category: bug
confidence: high
triage: confirmed-bug
status: open
feature: Node source packages/stats/src (feat_library_b8b0895f21)

evidence:
- packages/stats/src/main/stats.ts:39-54 (wilson)
- packages/stats/src/test/stats.test.ts:253-258

wilson exposes z as a caller-provided parameter but only validates k and n. Because z2 is squared while halfWidth keeps the original sign of z, passing a negative z makes halfWidth negative and returns lo greater than hi, violating the interval invariant and the documented lo/hi confidence-bound contract. The existing invalid-input tests cover k and n only, so this public parameter remains unchecked.

recommendation:
Validate z before computing the interval, for example require Number.isFinite(z) and z >= 0, and throw RangeError for invalid z values.

test analysis:
The validation suite asserts RangeError for invalid k and n values, but it never passes a custom z value or checks the interval invariant when z is negative or non-finite.

suggested regression test:
Add cases asserting wilson(5, 10, -1.96), wilson(5, 10, Number.NaN), and wilson(5, 10, Number.POSITIVE_INFINITY) throw RangeError, plus a valid custom z case if custom quantiles are supported.

minimum fix scope:
packages/stats/src/main/stats.ts validation in wilson plus focused tests in packages/stats/src/test/stats.test.ts

repro:
Call wilson(5, 10, -1.96). The calculation produces the same center as the default case but a negative halfWidth, so lo becomes the default upper bound and hi becomes the default lower bound.

## low: Build does not clear packaged dist output before compiling

id: fnd_sig-feat-release-4906432e7b-8bda_c2d833093c
category: build-release
confidence: medium
triage: risk
status: open
feature: Package script build (@codenuke/exec) (feat_release_4906432e7b)

evidence:
- packages/exec/package.json:16-21 (scripts.build)

The package only includes dist in release artifacts, and prepack delegates to the build script. The build script runs TypeScript compilation but does not remove the existing dist directory first, so files emitted by older source layouts can remain in dist and be packed even after the corresponding source is deleted or renamed. That can ship stale runtime or type files alongside the current build output.

recommendation:
Make the package build start from a clean output directory, for example by adding a clean step before tsc or changing prepack to run clean then build.

test analysis:
No linked tests are included for this package script, and ordinary unit tests would not detect stale files surviving in dist unless packaging output is explicitly inspected.

suggested regression test:
Add a packaging smoke test that creates a stale file in packages/exec/dist, runs the package build/prepack path, and asserts the stale file is absent from the packed artifact or final dist.

minimum fix scope:
Update packages/exec/package.json scripts to clean dist before invoking tsc, plus add a focused packaging/build test if the workspace has package smoke coverage.

## low: Fallback program path resolves outside the current directory

id: fnd_sig-feat-service-0759070955-dd97_296569f683
category: bug
confidence: medium
triage: risk
status: open
feature: Node source packages/config/src (feat_service_0759070955)

evidence:
- packages/config/src/main/config.ts:76-80 (runtimeModuleUrl)
- packages/config/src/main/config.ts:319 (loadConfig)
- packages/config/src/test/config.test.ts:216-223

When no module URL is supplied, runtimeModuleUrl falls back to pathToFileURL(realpathSync(process.argv[1] ?? process.cwd())). If process.argv[1] is absent and process.cwd() is a directory, pathToFileURL returns a directory path without a trailing slash, so programPathFromModuleUrl resolves ./program.md as a sibling of the cwd rather than inside it. The package-local path is covered for the normal import.meta.url path, but the cwd fallback path is not. This can produce a wrong default Config.program in embedded or unusual runtimes where process.argv[1] is unset.

recommendation:
When falling back to cwd, convert it to a directory URL with a trailing slash, or avoid the cwd fallback for resolving package-local runtime data. Keep file module paths and directory fallbacks handled separately.

test analysis:
The existing program.md tests cover import.meta.url and URL-decoding behavior, but they do not simulate process.argv[1] being absent or verify the cwd fallback branch.

suggested regression test:
Add a test for programPath/runtime fallback behavior with an argv-less runtime path, or expose a small helper that accepts an undefined module URL plus cwd and asserts the resulting program path is inside that cwd/package directory as intended.

minimum fix scope:
Adjust runtimeModuleUrl/program path fallback handling and add one targeted test for the process.argv[1] absent case.

## low: Implemented scorer commands are omitted from the recognized-command error text

id: fnd_sig-feat-cli-command-e103438025-_b2a85efc22
category: api-contract
confidence: medium
triage: contract-mismatch
status: open
feature: Node source apps/cli/src (feat_cli-command_e103438025)

evidence:
- apps/cli/src/main/cli.ts:65-73
- apps/cli/src/main/cli.ts:82-85

The dispatcher has a working scorer adapter when commandTarget resolves to the scorer module, but the error message for other recognized-but-unimplemented commands lists supported commands without mentioning scorer. Users who hit an unimplemented recognized command get inaccurate CLI contract information and may assume scorer is not available.

recommendation:
Include the scorer command names or a generic scorer reference in the supported-command message, ideally from the same command metadata used by commandTarget/cliHelpText so this cannot drift again.

test analysis:
No linked tests are included for the CLI dispatcher or its user-facing error output.

suggested regression test:
Add a CLI dispatcher test for a recognized unimplemented command that asserts the supported-command list includes every implemented adapter family, including scorer.

minimum fix scope:
Update the recognized-but-unimplemented error message or derive it from centralized command metadata.

## low: Package typecheck does not validate the build TypeScript project

id: fnd_sig-feat-release-1ae0636f10-695e_ba75e33302
category: build-release
confidence: medium
triage: risk
status: open
feature: Package script typecheck (@codenuke/stats) (feat_release_1ae0636f10)

evidence:
- packages/stats/package.json:20-23 (scripts)

The package build script compiles with tsconfig.build.json, but the typecheck script runs tsc --noEmit against tsconfig.json. If those configs include different files or compiler options, the advertised package typecheck can pass while the actual release build still fails. That weakens this script as a release gate for @codenuke/stats.

recommendation:
Align the typecheck script with the build project, or add an additional build-config typecheck such as tsc --noEmit -p tsconfig.build.json if tsconfig.json is intentionally broader for development/tests.

test analysis:
No linked tests or packaging checks are included for this feature, and the package script itself is the only evidence shown.

suggested regression test:
Add a package-level release smoke or workspace script test that runs the @codenuke/stats typecheck and build scripts and fails if either TypeScript project is invalid.

minimum fix scope:
Update packages/stats/package.json scripts only.

## low: Packaging smoke test is pinned to a stale tarball filename

id: fnd_sig-feat-library-760394befa-fc67_b8e6ec1559
category: build-release
confidence: medium
triage: risk
status: open
feature: Node package codenuke-workspace (feat_library_760394befa)

evidence:
- AGENTS.md:30-33

The packaging smoke instructions install `/tmp/codenuke-pack/codenuke-0.4.0.tgz` after running `npm pack ./apps/cli`. That filename changes whenever the CLI package version changes, so the documented smoke test will fail even when packing succeeds unless the maintainer manually updates this line for every release. The root manifest also identifies this package as only the private workspace orchestrator, so the fixed CLI tarball name is not derived from package metadata here.

recommendation:
Avoid hard-coding the tarball version in the smoke command. Capture the `npm pack` output or install via a glob constrained to the pack directory, for example the single generated `codenuke-*.tgz` file.

test analysis:
No linked tests are included for release packaging instructions, and this is a documentation/scripted release workflow hazard rather than runtime behavior.

minimum fix scope:
Update the packaging smoke instructions in `AGENTS.md` to derive the generated tarball path instead of referencing `codenuke-0.4.0.tgz`.

## low: Published package exposes a build script that points to an omitted file

id: fnd_sig-feat-release-193f3990ad-6075_c71073cd3e
category: build-release
confidence: medium
triage: risk
status: open
feature: Package script build (codenuke) (feat_release_193f3990ad)

evidence:
- apps/cli/package.json:14-21
- apps/cli/package.json:22-28

The public package includes an npm `build` script that runs `node scripts/bundle-cli.mjs`, but the package `files` allowlist only publishes `dist`, docs, and release metadata. If a user or downstream release process runs `npm run build` from the installed tarball, the script target will be absent and the command will fail. This is not a runtime CLI failure because `bin` points at `dist/cli.cjs`, so severity is low.

recommendation:
Either include the needed `scripts` build file in the published package or avoid exposing a `build` script in the published package if rebuilding from the tarball is intentionally unsupported.

test analysis:
No linked tests or packaging smoke evidence are included for running package scripts from the produced tarball.

suggested regression test:
Add a packaging smoke test that packs the CLI package, installs the tarball into a temporary prefix, and either verifies `npm run build` works or asserts the published package intentionally does not expose a rebuild script.

minimum fix scope:
Update `apps/cli/package.json` package contents or scripts.

repro:
Pack and install the package, then run `npm run build` inside the installed package directory; Node will not find `scripts/bundle-cli.mjs` because it is not included by the package allowlist.

## low: Run iteration parsing silently changes zero and invalid counts to five

id: fnd_sig-feat-cli-command-e103438025-_9a9a4c4c72
category: bug
confidence: medium
triage: risk
status: open
feature: Node source apps/cli/src (feat_cli-command_e103438025)

evidence:
- apps/cli/src/main/cli.ts:75-77

The `Number(rest[0]) || 5` defaulting treats `0`, `NaN`, and an empty string the same as an omitted argument. That means `codenuke run 0` unexpectedly runs five iterations instead of zero or returning a validation error, and malformed values also proceed with the default without telling the user.

recommendation:
Parse the optional iteration argument explicitly: default only when the argument is absent, reject non-finite/non-integer/negative values with a clear exit code, and pass a valid parsed number through unchanged.

test analysis:
No linked tests are included for `run`/`loop` argument parsing or invalid iteration input.

suggested regression test:
Add dispatcher tests covering omitted iterations, `0`, a positive integer, and an invalid string for both `run` and `loop`.

minimum fix scope:
Replace truthiness-based defaulting for `iterations` with explicit argument validation in the run/loop branch.

